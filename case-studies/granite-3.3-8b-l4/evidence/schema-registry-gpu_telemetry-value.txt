# gpu_telemetry-value schema + compatibility (Schema Registry, Confluent Cloud)
## compatibility level:
| Compatibility Level | BACKWARD |
## schema:
Schema ID: 100001
Type: AVRO
Schema:
{
    "type": "record",
    "name": "GpuInferenceTelemetry",
    "namespace": "io.lutflow.demo.v1",
    "doc": "Per-scrape GPU inference telemetry. Every field maps 1:1 to a real, public metric from vLLM (Prometheus v1) + NVIDIA DCGM exporter + OpenTelemetry semantic conventions (GenAI + Hardware/GPU). The data in this demo is SYNTHETIC (synthetic structured producer; see produce.py); the production source is an OpenTelemetry Collector / Prometheus->Kafka bridge emitting the same schema, so the pipeline is unchanged. This subset contains only standards-grounded fields and nothing proprietary.",
    "fields": [
        {
            "name": "deployment_id",
            "type": "string",
            "doc": "OTel resource: service.instance.id. Generic identifier of an inference deployment."
        },
        {
            "name": "model_id",
            "type": "string",
            "doc": "OTel gen_ai.request.model. Generic model name."
        },
        {
            "name": "gpu_uuid",
            "type": "string",
            "doc": "DCGM label gpu_uuid / OTel hw.id."
        },
        {
            "name": "gpu_model",
            "type": "string",
            "doc": "DCGM modelName / OTel hw.name (e.g. NVIDIA L4)."
        },
        {
            "name": "ts",
            "type": "long",
            "doc": "Scrape timestamp as epoch milliseconds (UTC). Kept as a plain long so Flink can convert it to event time with TO_TIMESTAMP_LTZ(ts, 3) (a logicalType is intentionally NOT used: it must not depend on the Datagen connector preserving it)."
        },
        {
            "name": "gpu_util_pct",
            "type": "double",
            "doc": "DCGM_FI_DEV_GPU_UTIL / OTel hw.gpu.utilization (0-100)."
        },
        {
            "name": "sm_active_ratio",
            "type": "double",
            "doc": "DCGM_FI_PROF_SM_ACTIVE - streaming-multiprocessor occupancy (0-1)."
        },
        {
            "name": "tensor_active_ratio",
            "type": "double",
            "doc": "DCGM_FI_PROF_PIPE_TENSOR_ACTIVE - Tensor Core activity (0-1)."
        },
        {
            "name": "dram_active_ratio",
            "type": "double",
            "doc": "DCGM_FI_PROF_DRAM_ACTIVE - memory-bandwidth utilization (0-1)."
        },
        {
            "name": "fb_used_mib",
            "type": "double",
            "doc": "DCGM_FI_DEV_FB_USED - framebuffer / VRAM used (MiB)."
        },
        {
